# 任务未完成记录

更新时间：2026-04-27

## 未完成任务 1
- 目标：OpenVoice 与 RVC 两条模型链路都要完整跑通（ASR -> 改写 -> Gold Teacher -> 音色转换）。
- 当前状态：未完成。
- 未完成原因：RVC 独立环境安装与依赖冲突尚未闭环，导致双模型并行可用状态未达成。
- 处理约定：已暂停本次安装与运行，待稍后重新启动并继续闭环。

## 工作状态 2：文化悬浮百科
- 目标：在语义转写层展示固定的方言文化知识闪卡，用于解释特殊方言词、俗语、文化背景和使用语境。
- 当前状态：已完成第一版代码实现，待合并后结合真实 Demo 样例继续扩充词条。
- 覆盖范围：第一版覆盖粤语和四川话，闽南语暂不纳入。
- 展示形式：Web Demo 在“语义转写文本”下方新增“文化百科卡片”Markdown 区域，展示命中片段高亮和解释卡片，不做复杂 hover 浮窗。
- 数据来源：固定写在 `FireRedASR2S/fireredasr2s/dialect_pipeline/cultural_cards.py`，运行时不依赖联网、RAG 或 LLM。
- 接口变更：`RewriteResponse` 新增 `cultural_cards` 和 `cultural_card_terms`，完整 pipeline 会随 `rewrite` 一起返回。
- 处理约定：文化卡只作为展示和调试解释层，不进入 TTS 输入，不改变 Gold Teacher / Voice Matched 主音频链路。

